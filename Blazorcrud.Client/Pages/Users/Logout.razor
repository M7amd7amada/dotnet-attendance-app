@page "/user/logout"
@inject IUserService UserService

@code {
    protected override async void OnInitialized()
    {
        UserService.User.LogoutDate = DateTime.Now;
        StateHasChanged();
        await UserService.Logout();
    }
}